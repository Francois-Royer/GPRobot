package sample;

public class %s extends GPBase {

    @Override
    public void doGP () {
        // --- PHENOME 1 ---
        fire = AvoidNan( % s, 0);

        // --- PHENOME 2 ---
        turnGunLeft = AvoidNan( % s, 0);

        // --- PHENOME 3 ---
        //turnLeft=AvoidNan(#s, 0);

        // --- PHENOME 4 ---
        //ahead=AvoidNan(#s, 0);
    }
}